/*const routes = {
  home: "/",
  search: "/search",
  test: "/test",
};

export default routes;*/

const routes = {
  home: "/",
  search: "/search",
  test: "/test",
  result: "/result",
  related: "/related",
};

export default routes;
